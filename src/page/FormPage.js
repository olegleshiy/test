import React from 'react';

import { Header, Form } from '../components';

const FormPage = () => {
    return (
        <div>
            <Header />
            <Form />
        </div>
    );
};

export default FormPage;
